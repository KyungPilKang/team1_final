<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.finalproject.dao.AdminDAO">
	
	<select id="qnaReqList" resultType="Request">
		<![CDATA[
			select * from request order by request_num
		]]>
	</select>
	
	<select id="qnaAnsList" resultType="Answer">
		<![CDATA[
			select * from answer order by answer_num
		]]>
	</select>

</mapper>