<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.finalproject.dao.FreelanceDAO">
	<insert id="regFreelance_freeType1" parameterType="Freelance">
		<![CDATA[
			insert into freelance values (#{no}, #{member_no}, #{name}, #{region}, #{gender}, #{age}, #{age_section}, 
			#{email}, #{phone}, #{freelance_type}, #{labor_type}, #{labor_time}, #{career}, #{grade}, #{class_type}, 
			#{class_type_etc}, #{education}, #{univ}, #{major}, #{certification_file_name}, #{camera}, #{camera_etc},
			#{lecture_video}, #{lecture_video_etc}, #{lecture_video_format}, #{editor_tool}, #{editor_tool_etc},
			#{lecture_edit}, #{lecture_edit_etc}, #{lecture_edit_format}, #{introduce_title}, #{introduce_contents},
			#{promotion_video_file_name}, #{banner_image_file_name}, #{month_pay}, #{week_pay}, #{case_pay}) 
        ]]>
	</insert>
</mapper>

<!-- 
insert into freelance (no, member_no, name, region, gender, age, age_section, email, phone,
			freelance_type, labor_type, labor_time, career, grade, class_type, class_type_etc, education,
			univ, major, certification_file_name, introduce_title, introduce_contents, promotion_video_file_name,
			banner_image_file_name, ) values ()
 -->
<!-- 
CREATE TABLE `freelance` (
  `no` int NOT NULL AUTO_INCREMENT,
  `member_no` varchar(100) default null,
  `name` varchar(100) default null,
  `region` varchar(100) default null,
  `gender` varchar(100) default null,
  `age` varchar(100) default null,
  `age_section` varchar(100) default null,
  `email` varchar(100) default null,
  `phone` varchar(100) default null,
  `freelance_type` varchar(100) default null,
  `labor_type` varchar(100) default null,
  `labor_time` varchar(100) default null,
  `career` varchar(100) default null,
  `grade` varchar(100) default null,
  `class_type` varchar(100) default null,
  `class_type_etc` varchar(100) default null,
  `education` varchar(100) default null,
  `univ` varchar(100) default null,
  `major` varchar(100) default null,
  `certification_file_name` varchar(1000) default null,
  `camera` varchar(100) default null,
  `camera_etc` varchar(100) default null,
  `lecture_video` varchar(100) default null,
  `lecture_video_etc` varchar(100) default null,
  `lecture_video_format` varchar(100) default null,
  `editor_tool` varchar(100) default null,
  `editor_tool_etc` varchar(100) default null,
  `lecture_edit` varchar(100) default null,
  `lecture_edit_etc` varchar(100) default null,
  `lecture_edit_format` varchar(100) default null,
  `introduce_title` varchar(1000) default null,
  `introduce_contents` mediumtext default null,
  `promotion_video_file_name` varchar(1000) default null,
  `banner_image_file_name` varchar(1000) default null,
  `month_pay` varchar(100) default null,
  `week_pay` varchar(100) default null,
  `case_pay` varchar(100) default null,
  PRIMARY KEY (`no`)
) ENGINE=InnoDB AUTO_INCREMENT=26 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci;
 -->